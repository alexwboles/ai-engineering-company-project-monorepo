# HealthCore Web Incident Analysis UI

## Run

Serve this folder with any static file server:

```bash
cd uis/web
python -m http.server 5500
```

Open `http://localhost:5500`.

The UI expects the API to be running at `http://localhost:8000`.
